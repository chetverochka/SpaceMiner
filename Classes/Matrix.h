#ifndef __MATRIX_H__
#define __MATRIX_H__

#include <cstdint>
#include <vector>

//template<typename T>
//class Matrix {
//public:
//	Matrix(const size_t x, const size_t y);
//	Matrix();
//	Matrix(const Matrix& matrix);
//	~Matrix();
//
//	T& operator()(const size_t x, const size_t y);
//
//	void setDimensions(const size_t width, const size_t height);
//
//	void getDimensions(size_t& width, size_t& height) const;
//	const size_t& getWidth() const					{ return m_width; }
//	const size_t& getHeight() const					{ return m_height; }
//	const size_t& getCapacity() const				{ return m_data.capacity(); }
//
//private:
//	size_t m_width;
//	size_t m_height;
//
//	std::vector<T> m_data;
//};

#endif //!__MATRIX_H__
